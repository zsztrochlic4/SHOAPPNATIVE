# Cloud Functions build + deps (never committed)
node_modules/
# Anchored to the functions root so it only ignores the COMPILED output
# (functions/lib), NOT the source directory functions/src/lib.
/lib/
# Generated by scripts/sync-shared.mjs — a verbatim copy of the shared coach
# safety+engine source (single source of truth lives in src/backend/**). Never
# committed, never hand-edited; regenerated on every build.
/src/_shared/
*.log
.env
.env.*
!.env.example
