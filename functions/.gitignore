# Cloud Functions build + deps (never committed)
node_modules/
# Anchored to the functions root so it only ignores the COMPILED output
# (functions/lib), NOT the source directory functions/src/lib.
/lib/
*.log
.env
.env.*
!.env.example
