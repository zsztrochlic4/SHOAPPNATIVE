import { View } from 'react-native'

/**
 * Progress screen — intentionally left blank.
 *
 * The content was removed on request; the tab and navigation stay in place so the
 * rest of the app is unaffected. Only App.tsx consumes this file (the default
 * export), so blanking it touches no other section. The previous implementation is
 * preserved in git history if it's ever needed back.
 */
export default function Progress() {
  return <View className="flex-1" />
}
